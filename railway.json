{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting V-Try API...' && echo 'DATABASE_URL:' $DATABASE_URL && echo 'JWT_SECRET:' ${JWT_SECRET:+SET} && (npx prisma db push --accept-data-loss --skip-generate || echo 'DB setup failed, starting anyway...') && npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
