{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting V-Try API DEBUG...' && echo 'All Environment Variables:' && env | grep -E '(DATABASE|REDIS|JWT|NODE|PORT)' && echo 'Testing Node.js...' && node -e \"console.log('Node.js works!')\" && echo 'Testing npm...' && npm --version && echo 'Listing files...' && ls -la && echo 'Testing debug script...' && npm run debug",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
