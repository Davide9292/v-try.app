{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting V-Try API DEBUG...' && echo 'DATABASE_URL:' $DATABASE_URL && echo 'JWT_SECRET:' ${JWT_SECRET:+SET} && npm run debug && echo 'Debug completed, starting main app...' && npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
